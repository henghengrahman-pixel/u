<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
  <meta name="theme-color" content="#0f1320">
  <meta name="format-detection" content="telephone=no">

  <%
    const clean = (value = '') => String(value || '').trim();

    const ensureAbsolute = (value = '') => {
      const raw = clean(value);
      if (!raw) return '';

      if (/^https?:\/\//i.test(raw)) {
        return raw;
      }

      const base = clean(baseUrl).replace(/\/+$/, '');
      if (!base) {
        return raw.startsWith('/') ? raw : `/${raw}`;
      }

      return `${base}${raw.startsWith('/') ? raw : `/${raw}`}`;
    };

    const truncate = (value = '', max = 160) => {
      const text = clean(value).replace(/\s+/g, ' ');
      if (text.length <= max) return text;
      return `${text.slice(0, Math.max(0, max - 3)).trim()}...`;
    };

    const isAdminPage = clean(currentPath).startsWith('/admin');
    const isArticlePage = clean(currentPath).startsWith('/article/');

    const storeName = clean(settings?.storeName || 'MWG Oversize');
    const storeLogo = ensureAbsolute(settings?.logo || '/assets/images/logo.png');
    const defaultOgImage = ensureAbsolute(settings?.seo?.ogImage || '/assets/images/og-image.jpg');
    const faviconUrl = ensureAbsolute(settings?.favicon || settings?.logo || '/assets/images/logo.png');

    const pageTitle = clean(meta?.title || settings?.seo?.metaTitle || `${storeName} - Rekomendasi Kaos Pria Terbaik`);
    const pageDescription = truncate(
      meta?.description ||
      settings?.seo?.metaDescription ||
      'Temukan rekomendasi kaos pria terbaik, kaos oversize premium, dan fashion pria kekinian dengan bahan nyaman serta model yang cocok untuk daily outfit.',
      160
    );
    const pageKeywords = clean(
      meta?.keywords ||
      settings?.seo?.keywords ||
      'rekomendasi kaos pria, kaos oversize pria, kaos pria terbaik, kaos distro pria, fashion pria kekinian'
    );

    const canonicalUrl = ensureAbsolute(meta?.canonical || meta?.url || `${baseUrl}${currentPath || '/'}`);
    const pageUrl = ensureAbsolute(meta?.url || canonicalUrl);
    const pageImage = ensureAbsolute(meta?.image || defaultOgImage || storeLogo);
    const robotsValue = clean(
      meta?.robots ||
      (isAdminPage
        ? 'noindex,nofollow,noarchive'
        : 'index,follow,max-image-preview:large,max-snippet:-1,max-video-preview:-1')
    );

    const bodyClass = isAdminPage ? 'admin-page' : '';

    const googleVerification = clean(settings?.googleVerification || '');
    const analyticsSnippet = clean(settings?.analytics || '');
    const gaMeasurementId = clean(process.env.GA_MEASUREMENT_ID || settings?.gaMeasurementId || 'G-PNZX11W2JE');

    const structuredList = Array.isArray(structuredData)
      ? structuredData.filter(Boolean)
      : (structuredData ? [structuredData] : []);

    const hasSchemaType = (type) => {
      return structuredList.some((schema) => {
        const schemaType = schema && schema['@type'];
        if (Array.isArray(schemaType)) return schemaType.includes(type);
        return schemaType === type;
      });
    };

    const defaultSchemas = [];

    if (!isAdminPage && !hasSchemaType('WebSite')) {
      defaultSchemas.push({
        '@context': 'https://schema.org',
        '@type': 'WebSite',
        name: storeName,
        url: clean(baseUrl),
        potentialAction: {
          '@type': 'SearchAction',
          target: `${clean(baseUrl)}/shop?q={search_term_string}`,
          'query-input': 'required name=search_term_string'
        }
      });
    }

    if (!isAdminPage && !hasSchemaType('Organization')) {
      defaultSchemas.push({
        '@context': 'https://schema.org',
        '@type': 'Organization',
        name: storeName,
        url: clean(baseUrl),
        logo: storeLogo
      });
    }

    const schemaList = [...defaultSchemas, ...structuredList];

    const articlePublishedAt = clean(
      typeof article !== 'undefined'
        ? (article?.publishedAt || article?.published_at || article?.createdAt || article?.created_at || article?.date || '')
        : ''
    );

    const articleModifiedAt = clean(
      typeof article !== 'undefined'
        ? (article?.updatedAt || article?.updated_at || article?.modifiedAt || article?.modified_at || article?.publishedAt || article?.createdAt || article?.created_at || article?.date || '')
        : ''
    );
  %>

  <title><%= pageTitle %></title>

  <meta name="description" content="<%= pageDescription %>">
  <meta name="keywords" content="<%= pageKeywords %>">
  <meta name="robots" content="<%= robotsValue %>">
  <link rel="canonical" href="<%= canonicalUrl %>">

  <% if (googleVerification && !isAdminPage) { %>
    <meta name="google-site-verification" content="<%= googleVerification %>">
  <% } %>

  <link rel="alternate" href="<%= canonicalUrl %>" hreflang="id-ID">
  <link rel="alternate" href="<%= canonicalUrl %>" hreflang="x-default">

  <% if (faviconUrl) { %>
    <link rel="icon" href="<%= faviconUrl %>">
    <link rel="shortcut icon" href="<%= faviconUrl %>">
    <link rel="apple-touch-icon" href="<%= faviconUrl %>">
  <% } %>

  <meta property="og:locale" content="id_ID">
  <meta property="og:site_name" content="<%= storeName %>">
  <meta property="og:type" content="<%= isArticlePage ? 'article' : 'website' %>">
  <meta property="og:title" content="<%= pageTitle %>">
  <meta property="og:description" content="<%= pageDescription %>">
  <meta property="og:url" content="<%= pageUrl %>">
  <meta property="og:image" content="<%= pageImage %>">
  <meta property="og:image:secure_url" content="<%= pageImage %>">
  <meta property="og:image:alt" content="<%= pageTitle %>">

  <% if (isArticlePage && articlePublishedAt) { %>
    <meta property="article:published_time" content="<%= articlePublishedAt %>">
  <% } %>

  <% if (isArticlePage && articleModifiedAt) { %>
    <meta property="article:modified_time" content="<%= articleModifiedAt %>">
  <% } %>

  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:title" content="<%= pageTitle %>">
  <meta name="twitter:description" content="<%= pageDescription %>">
  <meta name="twitter:image" content="<%= pageImage %>">
  <meta name="twitter:image:alt" content="<%= pageTitle %>">

  <% if (!isAdminPage && !analyticsSnippet && gaMeasurementId) { %>
    <link rel="preconnect" href="https://www.googletagmanager.com" crossorigin>
    <link rel="preconnect" href="https://www.google-analytics.com" crossorigin>

    <script async src="https://www.googletagmanager.com/gtag/js?id=<%= gaMeasurementId %>"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', <%- JSON.stringify(gaMeasurementId) %>, {
        page_title: <%- JSON.stringify(pageTitle) %>,
        page_location: <%- JSON.stringify(pageUrl) %>
      });
    </script>
  <% } %>

  <% if (!isAdminPage && analyticsSnippet) { %>
    <%- analyticsSnippet %>
  <% } %>

  <% schemaList.forEach((schema) => { %>
    <script type="application/ld+json"><%- JSON.stringify(schema) %></script>
  <% }) %>

  <link rel="stylesheet" href="/assets/css/bootstrap.min.css">
  <link rel="stylesheet" href="/assets/css/font-awesome.min.css">
  <link rel="stylesheet" href="/assets/css/style.css">
</head>
<body class="<%= bodyClass %>">
